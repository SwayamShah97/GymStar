<h1>Change your review
</h1>
<main>
    <form action = "/editeReview" method="POST" id="edite_reivew">

        <label for="your_reivewE" id="reviewInfoEdite">Leave your review here:</label><br>
        <input type="text" id="your_reivewE" name="review"><br>

        <label for="your_ratingE" id="ratingNumEdite">Give a score(1~5, 5 is the highest)</label><br>
        <input type="text" id="your_ratingE" name="rating"><br>

        <input type="submit" name="submit" value="submit">
    </form>

    {{#if ReviewNotProvide}}
    <p class = "error">You can not leave your review empty</p>
    {{/if}}

    {{#if reviewTypeWrong}}
    <p class = "error">Please enter the correct type review</p>
    {{/if}}

    {{#if reviewSapce}}
    <p class = "error">Your review can not be all white space </p>
    {{/if}}

    {{#if ratingNotProvide}}
    <p class = "error">You need to provide a rating</p>
    {{/if}}

    {{#if ratingTypeWrong}}
    <p class = "error">You need to give a rgiht type rating </p>
    {{/if}}

    {{#if ratingWrong}}
    <p class = "error">You need to give a valid number</p>
    {{/if}}







</main>